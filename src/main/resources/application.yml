server:
  port: '8089'
spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    database:
      platform: org.hibernate.dialect.MySQL8Dialect
  output:
    ansi:
      enabled : always