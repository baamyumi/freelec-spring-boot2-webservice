language: java
jdk:
    - openjdk17
branches:
  only:
    - main
#Travis CI  서버의 Home
#gradle을 통하여 의존성을 받게 되면 이를 캐시하여 배포할 때 마다 다시 받지 않도록 설정한다.
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'
script: ".gradlew clean build"

#CI 실행 완료 시 메일로 알림
notifications:
  email:
    recipients:
      - dbal5886@gmail.com
